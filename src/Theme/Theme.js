import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  Typography: {
    fontFamily: [
      "Graphik",
      //   'Helvetica',
      //   "sans-serif",
    ].join(","),
  },
});

export default theme;
